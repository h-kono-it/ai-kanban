# todo

今後やる予定のこと。手をつける前にここを見て、完了したら消すかチェックする。

## 機能

- [ ] **通知**（ブラウザの Notification API）。2026-08-15 に方針決定: Discord webhook は使わない。WebSocket が常時つながっているので `op` が届いた瞬間にブラウザから通知でき、サーバー側の送信処理もスケジューラも webhook 設定も要らない。`document.hasFocus()` で「自分が画面で操作した変更」と「外から届いた変更」を切り分けられるのが決め手（自分の操作で鳴らない）。期限通知も `dueDate` が `board` に載っているのでクライアント側の日次チェックで足りる。制約はタブを閉じていると通知が出ないこと（常時受信には Push API と外部プッシュサービスが必要になるので、そこまではやらない）。設計は ai-kanban のボード自身に細分化して入れてある。
- [ ] **完了ノードの自動削除**（30日）。**2026-08-22: 当面これは要らないかもしれない** — 完了列を「完了時刻の新しい順 + 7日より前は畳む」にしたので、キップル化の主症状（探せない・一覧性が無い）は消えた。実測で4稼働日35件＝8.75件/日ペースなので、畳みが効かなくなるとしたら数百件になってから。消す必要が出たら下記の設計判断が要る。上と同じ仕組みに乗せる。ただしノードは**ツリーの実体**なので、旧実装のように黙って消すと子ごと消える。「葉かつ完了から30日」に限るか、そもそも消さずにアーカイブ列へ送るかを先に決めること。
- [ ] **親ノードのステータス自動ロールアップ**。今は連動なしで、ツリーに `3/7` の進捗を出すだけ。「子が全部 done なら親も done」にするなら、子を1つ戻したときに親も戻すのか等の連鎖ルールを決めてから。
- [ ] **コメント / 変更履歴**。「なぜ却下したか」は `rejectNode` の `reason` で親の `description` に1行残るようにした（2026-08-17）が、残るのは却下のときだけ。「レビューで何を直してほしいか」を残す場所は無いままなので、AI に差し戻す運用を始めると欲しくなるはず。時系列のコメントをノードにぶら下げる形か、`GET /feedback?since=<seq>` で前回以降の指摘だけ返す形が候補。
- [ ] **MCP サーバー化**。今は REST を curl で叩く前提。MCP ツール（`list_goals` / `decompose` / `move_card` …）として公開すると Claude Code から直接呼べる。API.md の内容がそのままツール定義になる。
- [ ] **モバイルレイアウト**。未対応（旧 my-kanban と同じ）。
- [ ] **ボードごとの認証**。今はデプロイ全体で共有の `PASSPHRASE` 1本（未設定なら認証なし）。ローカル利用の間は不要。

- [ ] **提案を束で見せる・数える**。実際に 38 件の提案を投入して使ってみたところ、［配下ごと承認］を押して一括で通す流れになった（＝中身を1つずつ見ない）。要対応バッジも「要対応 38 件」と出るが、実態は「目的3つ分の提案」なので数字が実感とズレる。`提案 3件 / レビュー待ち 0件` のように束で数える、承認前に差分をまとめて見せる、といった改善余地がある。承認の取り消し（`unapproveNode`）を入れたので致命的ではなくなったが、「見ないまま通る」構造自体は残っている。

## 直したい細かいところ

- [ ] **カンバンからのカード追加の置き場所**。`ui.scopeId` が未設定だと `kind:"task"` のノードがルート直下（目的と同じ階層）にできる。素早く足せる利点はあるが、ツリーとしては据わりが悪い。「未分類」の目的を自動で作ってそこに入れる案がある。
- [ ] **作成した目的の id 特定が heuristic**。楽観更新しない設計なので、目的追加モーダルは「送信時に無かったルート id ＋ タイトル一致」で自分が作ったノードを拾っている（`public/modals.js` の `resolveCreatedGoal`）。同名の目的が同時に作られると取り違える。`Intent` にクライアント生成の相関 id を載せて `Op` で返せば確実になる。
- [ ] **`setListRole` は既存ノードの `completedAt` を再計算しない**。role を `done` に変えても、既にその列にいるノードには完了時刻が入らない（次に `moveCard` したときから効く）。role を変えただけで時刻が湧いたり消えたりする方が混乱すると判断したが、自動削除を入れるならここが効いてくる。
- [ ] **読み取り API が毎回ボード全体を読む**。`getTree` / `getAwaiting` / `getUndecomposedGoals` はどれも `loadBoard()` を土台にしている。ボード1つ分はメモリに載る前提なので今は問題ないが、ノードが数千を超えたら個別のクエリに置き換える。
- [ ] **UI 側のテストが無い**。`ops.ts` / `store.ts` は `npm test` で固めた（`test/`、20件）が、DnD・描画・`applyOp` は手動確認のまま。特に `public/state.js` の `applyOp` は `ops.ts` の鏡像なので、片方だけ直して壊れる余地が残っている。
- [ ] **`deleteList` の退避先が role を見ていない**。「position が最も小さい別の列」へ機械的に送るので、完了列を消すと通常列の末尾に完了済みタスクが並ぶ（完了時刻は落ちるので不整合ではないが、置き場所としては雑）。`role="normal"` の先頭を優先するなど。
- [ ] **承認したときのカンバンの並びが幅優先**。`approveNode` は `subtreeIds()`（`ORDER BY depth, tree_pos`）の順に列へ積むので、`機能A → 機能B → タスクA1 → タスクA2` の順に並ぶ。一方 `bulkAddNodes` は深さ優先で作るため、**投入順と承認順で並びが変わる**。ツリーの見た目と揃えるなら承認側を深さ優先にする。実害は小さい（カンバンの並びは元々ドラッグで変えるもの）。

## やらないと決めたこと

- **Cloudflare への再展開**。SQLite ファイルとローカル実行を前提に作り直したので、戻すなら D1 か Durable Object の SQL API への移植になる。ローカルの Claude Code から叩くのが主目的なので、当面その必要はない。
- **サーバー側で LLM を呼ぶ**。細分化の知能はローカルの Claude Code 側に置く、というのがこのプロジェクトの前提。UI に「細分化する」ボタンは作らない（押しても何も起きないボタンになる）。
