# このファイルを .env にコピーして使う。npm run dev / npm start が自動で読む。

# 待ち受けポート（未設定なら 3000）。
PORT=3000

# SQLite ファイルの場所（未設定なら ./data/kanban.db）。
# DB_PATH=./data/kanban.db

# 共有パスフレーズ。未設定なら認証は完全に無効（ローカル利用の既定）。
# ブラウザは HMAC クッキー、API は `Authorization: Bearer <PASSPHRASE>` で通す。
# PASSPHRASE=
